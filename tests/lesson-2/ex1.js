const number = 12;
let name = 'my number'; // (or var name = 'my number';)
let isEven = false; // (or var isEven = false;)
if (number % 2 === 0) {
    isEven = true;
}
console.log(name + ' is ' + isEven);